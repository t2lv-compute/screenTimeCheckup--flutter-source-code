name: screen_time_checkup
description: "A novel way to stay on track."
publish_to: "none"
version: 4.1.1+12

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  provider: ^6.1.5
  textfield_tags: ^3.0.1
  path_provider: ^2.1.1
  flutter_local_notifications: ^20.0.0
  android_alarm_manager_plus: ^5.0.0
  permission_handler: ^12.0.1
  web: ^1.0.0
  share_plus: ^12.0.1
  file_picker: ^10.3.10
  encrypt: ^5.0.3
  fl_chart: ^1.1.1
  confetti: ^0.8.0
  url_launcher: ^6.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  mocktail: ^1.0.0
  integration_test:
    sdk: flutter

flutter:
  uses-material-design: true
